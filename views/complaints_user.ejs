<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Raise Complaints</title>
    <link rel="icon" href="/images/logo.jpg">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
        }

        .header {
            background-color: #333;
            color: white;
            padding: 15px 0;
            display: flex;
            align-items: center;
            height: 70px;
            width: calc(100% - 0px);
            margin: 0 auto;
            border-radius: 0 0 10px 10px;
        }
        .header-text {
            flex-grow: 1;
            text-align: center;
            font-size: 24px;
            margin-left: -5px;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            font-size: 32px;
            margin-bottom: 20px;
            text-align: center;
            color: #e2ecee;
        }
        p {
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 20px;
            color: #555;
        }
        .query-container {
            display: none;
            margin-top: 20px;
        }
        .show {
            display: block;
        }
        .option {
            display: inline-block;
            margin-right: 20px;
            cursor: pointer;
            padding: 10px 20px;
            background-color: #333;
            color: white;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .option:hover {
            background-color: #555;
        }
        label {
            display: block;
            margin-bottom: 5px;
            color: #333;
            font-weight: bold;
        }
        input[type="text"],
        textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            resize: none;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="/images/logo.jpg" alt="Company Logo" class="logo" height="50px" width="150px" style="margin-left:10px ;" >
        <div class="header-text">
        </div>
        <div class="option" onclick="window.location.href='/userpage/<%=username%>'">Home</div>
    </div>

    <div class="container">
        <h2>Post your complaints here:</h2>
        <div class="options">
            <div class="option" onclick="showQuery('vehicles')">About Vehicles</div>
            <div class="option" onclick="showQuery('other')">Any Other</div>
        </div>
        <div id="vehicles" class="query-container">
            <h3>About Vehicles:</h3>
            <form action="/complaintvehicle/<%=username%>" method="post">
                <label for="vehicle-name">Vehicle Name:</label>
                <input type="text" id="vehiclename" name="vehiclename">
                <label for="vehicle-issue">Issue:</label>
                <textarea id="vehicleissue" name="vehicleissue" rows="4" cols="50"></textarea>
                <button style="width: 120px; height: 30px; color: black; background-color: rgb(194, 241, 9); cursor: pointer;" type="submit" class="submit" onclick="show()">Submit</button>
            </form>
        </div>
        <div id="other" class="query-container">
            <h3>Any Other:</h3>
            <form action="/complaintothers/<%=username%>" method="post">
                <label for="other-issue">Issue:</label>
                <textarea id="otherissue" name="otherissue" rows="4" cols="50"></textarea>
                <button style="width: 120px; height: 30px;color: black; background-color: rgb(194, 241, 9); cursor: pointer;" type="submit" class="submit" onclick="show()">Submit</button>
            </form>
        </div>
    </div>
    <div><h3 style="color: green; margin-left: 380px;" id="success"></h3></div>
    <script>
        function show()
        {
          document.getElementById('success').innerText="Submitted successfully.."
        }
        document.getElementsByClassName('submit').addEventListener('click',()=>{
            document.getElementById('success').innerText='Submitted successfully';
        })
        function showQuery(queryType) {
            var containers = document.getElementsByClassName('query-container');
            for (var i = 0; i < containers.length; i++) {
                containers[i].classList.remove('show');
            }
            document.getElementById(queryType).classList.add('show');
        }
    </script>
</body>
</html>